{% extends 'base.html.twig' %}

{% block title %}Modifier Notification{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1>Modifier le paramètre de notification</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary mt-3">{{ button_label|default('Mettre à jour') }}</button>
        <a href="{{ path('admin_notification_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
    {{ form_end(form) }}
</div>
{% endblock %}
